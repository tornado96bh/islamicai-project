# الدفعة 2b — الربط، بقياس قبل/بعد

## التشغيل

```powershell
cd E:\IslamicAI_v3_full
& .\.venv\Scripts\Activate.ps1
# فُكّ الحزمة هنا

.\tools\20-wire-search.ps1 -DryRun
.\tools\20-wire-search.ps1
```

السكربت **يرفض العمل** إن لم يوجد `_eval\baseline.json`، ويقارن
تلقائياً بعد الربط. إن رصد انحداراً، يخبرك ألا تودع ويعطيك أمر التراجع.

---

## ما يتغيّر

| الملف | من | إلى |
|---|---|---|
| `ranking.py` | `score += hit_score` (جمع خام) | RRF بالرتبة |
| `fts.py` | عمود `text` مع `translate()` | `text_normalized` — يطابق الفهرس |
| `fuzzy.py` | `similarity() >= 0.18` | المعامل `%` — يستعمل فهرس GIN |
| `backfill` | تطبيع فقط | تطبيع + تصحيح OCR |

ثم يعيد بناء `text_normalized` لكل العناصر مع تصحيح OCR.
**`text_raw` لا يُمس.**

---

## لماذا هذي التغييرات تحديداً

قياسك الأساسي أظهر 3 من 5 نتائج أولى خاطئة:

| سؤال | النتيجة الأولى | العلة |
|---|---|---|
| إنما الأعمال بالنيات | `] ـ ١ [ ١٧ محمّـــــ د بـــــن يعقـــــوب` | تمديد OCR |
| اسماعيل | `] ٣١ [ ٢٥ ـ وعنـــــه ، عـــــن أحمـــــد` | تمديد OCR |
| الله | `. االله` | تراكم الدرجات |

**التمديد يرفع تشابه trigram زوراً** لأنه يولّد ثلاثيات متكررة
(`ـــ`, `ـــ`, `ـــ`). إزالته من فهرس البحث تُسقط هذه النتائج.

---

## نتيجة الاختبار على محاكاة سيناريوك

شظية طابقت 8 استعلامات مرشّحة، مقابل نص كامل ظهر في محركين:

```
1. [0.06950] الله ثمّ يدع عبادته .        rrf=0.02751  hit_count=2
2. [0.04929] عن أبي عبد الله ) عليه السلام (  rrf=0.01129  hit_count=2
3. [0.04123] . الله                        rrf=0.01148  خفض=-0.00825  hit_count=8
```

الشظية نزلت من المركز 1 إلى 3 **رغم 8 مطابقات**، لأن RRF يحتسبها مرة
واحدة داخل المحرك الواحد.

كل نتيجة الآن تحمل `score_explain` يبيّن مصدر كل جزء من درجتها.

---

## مكسب الأداء المتوقع

`fuzzy.py` كان يحسب `similarity()` لكل صف في الجدول، ثماني مرات لكل
بحث. المعامل `%` يستعمل فهرس GIN trigram الذي أنشأته الهجرة.

راقب `latency_ms_p50` في المقارنة. هذا أول مكان يظهر فيه أثر إصلاح
الفهارس من الدفعة الأولى.

---

## حالة الاختبار

| المكوّن | الحالة |
|---|---|
| منطق RRF على محاكاة سيناريوك | ✅ الشظية 1 -> 3 |
| مصحح OCR على 11 عيّنة من نصك | ✅ (الدفعة 2) |
| بناء الملفات الأربعة | ✅ `py_compile` |
| ترميز PowerShell | ✅ ASCII خالص |
| **`fuzzy.py` مع PostgreSQL حقيقي** | ⚠️ **لم يُشغَّل** |
| **`SET LOCAL pg_trgm.similarity_threshold`** | ⚠️ **لم يُختبَر** |

المعامل `%` وضبط العتبة هما أكثر ما يقلقني — لا قاعدة بيانات عندي.
لو رجع `fuzzy` صفر نتائج، فالسبب على الأرجح أن ضبط العتبة لم يسرِ.
أخبرني وأعطيك بديلاً.
