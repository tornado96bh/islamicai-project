# الدفعة الخامسة — كلها من قياسك أنت

## التشغيل

```powershell
cd E:\IslamicAI_v3_full
& .\.venv\Scripts\Activate.ps1
.\tools\50-batch5.ps1 -DryRun
.\tools\50-batch5.ps1
```

---

## ١) `set_limit` لا يقبل double — البحث كان على المسار البطيء

في مخرجاتك:
```
تعذّر ضبط عتبة pg_trgm عبر set_limit:
  function set_limit(double precision) does not exist
العتبة السارية 0.3 أعلى من المطلوبة 0.18 — التحوّل إلى المسار الاحتياطي
```

`set_limit` معرّفة على `real` فقط، و SQLAlchemy يرسل `float` كـ
`double precision`. الإصلاح: `cast(0.18, REAL)`.

**زمنك الحالي 2413ms كان مع المسار الاحتياطي.** الآن يُفترض أن يستعمل
فهرس GIN فعلاً.

الحمد أن المسار الاحتياطي كان موجوداً — لولاه لرجع البحث صفر نتائج
بصمت. هذا ما تعنيه «لا فشل صامت».

## ٢) أخطاء التصنيف من عيّنة مراجعتك

| النص | كان | صار |
|---|---|---|
| `٠١ ـ الزهد / ٢٧ : ٢٩١ .` | matn | **footnote** |
| `٧ ـ الفقيه . ١١ / ٨ : ١` | footnote ✓ | footnote |
| `ُكتاب الم ضارب ة .` | matn | **heading** |
| `الســعدآبادي ، عــن احمــد بــن أبي عبــد االله` | unknown | **sanad** |

**القاعدة الجديدة بنيوية لا قائمة أسماء:** `رقم ـ مصدر : أرقام` تُصنَّف
هامشاً حتى لو كان المصدر غير مذكور في أي قائمة عندي. اختبار يحرس هذا.

وأضفت شرط **كثافة الأرقام**: السطر الذي أكثر من 15% منه أرقام لا
يبتلعه الاحتياطي النثري.

## ٣) فلتر الكيانات — مربوط أخيراً

كُتب في الدفعة الثالثة ولم يُربط. الآن `entities.py` يمرّر كل مرشّح
عبره:

| المرشّح | النتيجة |
|---|---|
| `من الباب` | ✘ مرفوض |
| `في الحديث` | ✘ مرفوض |
| `عن أحمد بن محم د` | ✔ → `أحمد بن محم د` |
| `عن سعد بن عبد الله` | ✔ → `سعد بن عبد الله` |
| `كتاب الطهارة` | ✔ [book] لا [candidate] |
| `زرارة بن أعين` | ✔ [person] |

كل مرشّح يخرج مع `filter_reason` و `original_label`.

---

## نتيجة الاختبار

- **29 اختبار جديد ✅**
- تصنيف التخطيط: **18/18** على عيّنات من متنك
- السكربت يجرّب استعلاماً حقيقياً قبل القياس، ويطبع
  `index operator active: True/False`

---

## ما أتوقعه

| المقياس | قبل | متوقَّع |
|---|---|---|
| `latency_ms_p50` | 2413 | **أقل بوضوح** |
| `unknown` في التصنيف | 20.2% | أقل |
| `matn` | 39.0% | أقل قليلاً (الإحالات خرجت منه) |
| `footnote` | 20.8% | أعلى |

**لو بقي `index operator active: False`** أرسل لي المخرجات فوراً —
يعني `set_limit` ما زالت تفشل لسبب آخر.
