# ابدأ من هنا — حزمة الإصلاح v2

## ما تغيّر عن النسخة الأولى

النسخة الأولى فشلت لأني كتبت ملفات `.ps1` بترميز **UTF-8 بدون BOM**.
هذا صحيح لملفات بايثون، لكن **Windows PowerShell 5.1 يقرأ ملفات `.ps1`
بترميز ANSI إن لم يكن فيها BOM** — فتحوّل العربي إلى `ظ…ظ„ظپ` وكسر
النصوص. غلطتي.

الحل في هذه النسخة: **كل سكربتات PowerShell أصبحت ASCII خالص**
(رسائلها إنجليزية). لا يوجد فيها حرف واحد غير ASCII، فيستحيل أن يتشوّه
الترميز مهما كان إعداد ويندوز عندك. النصوص العربية انتقلت إلى مجلد
`patch\snippets\` وتُقرأ بترميز UTF-8 صريح.

---

## خطوات التركيب بالضبط

### 1) نظّف المحاولة السابقة
```powershell
cd E:\IslamicAI_v3_full
Remove-Item -Recurse -Force .\tools, .\patch -ErrorAction SilentlyContinue
```

### 2) فُكّ الحزمة الجديدة هنا
بحيث يصير عندك:
```
E:\IslamicAI_v3_full\
├── tools\      <- 9 سكربتات PowerShell
├── patch\      <- ملفات بايثون + snippets
├── packages\  apps\  alembic\  ...
└── pyproject.toml
```

### 3) اسمح بتشغيل السكربتات (لهذه النافذة فقط)
```powershell
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
```

### 4) عاين أولاً — لا يُكتب أي شيء
```powershell
cd E:\IslamicAI_v3_full
.\tools\RUN-ALL.ps1 -DryRun
```

### 5) إذا كانت المعاينة سليمة، نفّذ
```powershell
.\tools\RUN-ALL.ps1
```
سيطلب منك كتابة `yes` للتأكيد، ويأخذ نسخة احتياطية أولاً.

### 6) هجرة القاعدة (منفصلة عمداً)
```powershell
.\tools\05-migrations.ps1 -Apply
```

---

## ملاحظات مهمة

**PowerShell وليس CMD.** الأوامر أعلاه لا تعمل في `cmd.exe`.
افتح **Windows PowerShell** من قائمة ابدأ، أو داخل VS Code
اضغط `` Ctrl+` `` ثم اختر PowerShell من القائمة المنسدلة للطرفية.

**الأوامر السابقة فشلت** لأنها كانت أمثلة فيها نص نائب:
`/path/to/...` و `D:\...\` — استبدلها دائماً بمسارك الحقيقي
`E:\IslamicAI_v3_full`. وأوامر `xargs` و `du` و `tail` أوامر لينكس
ولا وجود لها في ويندوز.

**التراجع الكامل:**
```powershell
git reset --hard pre-fix-<الطابع-الزمني>
```
الوسم يطبعه سكربت `00-backup.ps1` في نهايته.

---

## ما تفعله كل خطوة

| السكربت | الوظيفة | يلمس القاعدة؟ |
|---|---|---|
| `00-backup.ps1` | وسم Git + نسخة storage + pg_dump | قراءة فقط |
| `01-secrets.ps1` | تدوير 3 كلمات مرور، إخراج `.env` من التتبع | لا |
| `02-purge.ps1` | إزالة ~79MB مشتقات و24 ملف `.bak`، `.gitignore` جدّي | لا |
| `03-guardrails.ps1` | إيقاف 3 عمليات تدمّر البيانات | لا |
| `04-install-canonicalizer.ps1` | المطبّع العربي الموحّد + 32 اختباراً | لا |
| `05-migrations.ps1` | أعمدة جديدة + إصلاح فهارس البحث | **نعم، بـ `-Apply`** |
| `06-verify.ps1` | 15 فحصاً | قراءة فقط |

---

## حالة الاختبار

| المكوّن | الحالة |
|---|---|
| `arabic_canonicalizer.py` | ✅ 32/32 اختبار نجح فعلياً |
| منطق سكربت 03 | ✅ **حوكي كاملاً على نسخة من ريبوك** والملفات تُصرَّف بنجاح |
| إعادة التشغيل مرتين | ✅ يتخطى بأمان، لا يكرر التعديل |
| ترميز سكربتات PowerShell | ✅ ASCII خالص، متحقَّق بايت-بايت |
| تشغيل PowerShell فعلياً | ⚠️ لا بيئة ويندوز عندي |
| الهجرة و backfill | ⚠️ لا قاعدة بيانات عندي |

لهذا: **ابدأ بـ `-DryRun` دائماً.**
