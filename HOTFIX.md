# إصلاح عاجل — عطب المعاملة المجهضة في fuzzy.py

## السبب الجذري

```python
self.db.execute(text("SET LOCAL pg_trgm.similarity_threshold = :t"), {"t": ...})
```

أمر `SET` في PostgreSQL **لا يقبل معاملات مربوطة** (bind parameters).
فشل الأمر → دخلت المعاملة حالة abort → و`except: pass` عندي ابتلع
استثناء بايثون **دون أن ينظّف المعاملة**، فسقط كل استعلام بعده بـ
`InFailedSqlTransaction`.

غلطتان في سطرين: صيغة SQL خاطئة، ومعالجة استثناء صامتة أخفت الأثر.

## الإصلاح

| قبل | بعد |
|---|---|
| `SET LOCAL ... = :t` | `SELECT set_limit(0.18)` — تقبل معاملاً فعلاً |
| بلا عزل | داخل `SAVEPOINT` — الفشل لا يفسد المعاملة الأم |
| بلا تحقق | `show_limit()` يتحقق أن العتبة سرت فعلاً |
| `except: pass` | تسجيل تحذير + مسار احتياطي |
| بلا بديل | لو تعذّر خفض العتبة، يعود إلى `similarity()` — أبطأ لكنه لا يفقد نتائج بصمت |

## التشغيل

```powershell
cd E:\IslamicAI_v3_full
& .\.venv\Scripts\Activate.ps1
# فُكّ الحزمة هنا

.\tools\21-hotfix-fuzzy.ps1 -DryRun
.\tools\21-hotfix-fuzzy.ps1
```

السكربت الآن **يجرّب استعلاماً حقيقياً على قاعدتك قبل القياس**، ويطبع:

```
index operator active : True/False
hits returned         : N
```

- `True` + نتائج → الفهرس يعمل، توقّع تحسّن سرعة
- `False` + نتائج → المسار الاحتياطي، صحيح لكن بلا مكسب سرعة
- صفر نتائج → أرسل لي المخرجات

لا يُشغَّل القياس إلا بعد نجاح اختبار الدخان.

---

## لا تتراجع عن دفعة 2b

الملء نجح بالكامل قبل الانهيار، وأرقامه مهمة:

| | العدد |
|---|---|
| تمديد أُزيل | **65,655** |
| رباطات صُحّحت | 2,131 |
| كلمات لُحمت | 3,285 |
| عناصر تغيّر نصها المفهرَس | **7,818 من 13,916 — 56%** |

`ranking.py` و`fts.py` سليمان. العطب في `fuzzy.py` وحده.
