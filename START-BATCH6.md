# الدفعة السادسة — إعادة بناء طبقة التعلّم من النص المصحَّح

## التشغيل

```powershell
cd E:\IslamicAI_v3_full
& .\.venv\Scripts\Activate.ps1
.\tools\60-batch6.ps1 -DryRun
.\tools\60-batch6.ps1
```

---

## السبب الجذري الذي كشفته نتائجك

```json
"label": "محم د بن يعقوب عن"     ← "عن" زائدة، و"محم د" مفكّكة
"label": "الحسين بن سعيد عن"
```

`trainer.py` كان يتعلّم من `element.text` — **العمود الأصلي غير
المصحَّح** — لا من `text_normalized`. ولهذا:

| كلمة | تردد |
|---|---|
| `د` (حرف مفرد) | 16,359 |
| `محم` | 13,427 |
| `محمد` الصحيحة | 2,651 |

كل ما بُني على هذا المعجم ورث التلوّث: الاقتراحات، العبارات،
الكيانات، والاستعلامات المرشّحة.

## ما يتغيّر

| الملف | التغيير |
|---|---|
| `trainer.py` | يتعلّم من `text_normalized` المصحَّح، **ولا يتعلّم الكيانات من الهوامش** |
| `entity_filter.py` | يقطع أدوات التحمّل من الذيل، ويرفض إحالات الأقسام |
| `fts.py` | احتياطي OR حين لا يجد AND شيئاً |

ثم **يؤرشف ملفات التعلّم القديمة ويعيد التدريب كاملاً**.

---

## فخّ لغوي اكتشفته أثناء البناء

التطبيع يوحّد **`على` (حرف الجر) مع `علي` (الاسم)**، و`إلى` معهما.

فلمّا أدرجتُ `علي` في قائمة الأدوات، صار:

```
علي بن إبراهيم عن   ->   بن إبراهيم      ✘ بُتر أشهر اسم راوٍ
محم د بن علي بن     ->   محم د           ✘
```

أزلتها من القائمتين، وكتبت اختباراً يحرس هذا تحديداً:
`test_ali_is_never_stripped_as_a_preposition`.

**الأمان أولى من التنظيف** حين يتعارض الاثنان.

---

## أثر الفلتر على كياناتك

| قبل | بعد |
|---|---|
| `محم د بن يعقوب عن` | `محم د بن يعقوب` |
| `الحسين بن سعيد عن` | `الحسين بن سعيد` |
| `محم د بن علي بن` | `محم د بن علي` |
| `علي بن إبراهيم عن` | `علي بن إبراهيم` |
| `من أبواب أحكام` | **مرفوض** |
| `أبواب نواقض الوضوء` | **مرفوض** |
| `كتاب الطهارة` | يبقى [book] |

قاعدة الرفض **بنيوية**: الاسم الذي يبدأ باسم عام (`باب`، `أبواب`،
`الحديث`) وليس مؤشر عنوان (`كتاب`) هو إحالة قسم لا كيان.

---

## احتياطي OR في البحث

`websearch_to_tsquery` يربط الكلمات بـ **AND**. فاستعلام
«زرارة بن أعين» يتطلب الكلمات الثلاث في **العنصر نفسه**. ومع تفكك
OCR وانقسام الأسطر لا يجتمعن، فيرجع صفراً وتغرق الاستعلاماتُ
المرشّحة العامة النتيجةَ — وهذا ما رأيته في `q001` و`q002`.

الآن: عند خلوّ نتيجة AND يُعاد بـ OR مع وسم `full text (or)`.

---

## نتيجة الاختبار

**19 اختبار جديد ✅**، وتنظيف الكيانات **13/13** على حالات من مخرجاتك.

## ما أتوقعه

| المقياس | الآن | متوقَّع |
|---|---|---|
| `must_contain_rate` | 0.50 | أعلى (احتياطي OR) |
| أسماء الكيانات | `محم د بن يعقوب عن` | `محمد بن يعقوب` |
| `candidate_queries` | فيها تفكك | أنظف |

**ملاحظة:** `محم د` لن تصير `محمد` إلا بعد إعادة التدريب — لأن
التصحيح يلحمها في `text_normalized`، والتدريب يقرأ منه الآن.
