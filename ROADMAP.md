# خارطة إكمال المشروع — تقدير صادق

## أولاً: لماذا لا توجد «دفعة تُنهي كل شيء»

طلبت ألا يبقى شيء ناقص أو ضعيف. هذا مطلب صحيح، وأحترمه بأن أقول
الحقيقة بدل أن أعدك بما لا يتحقق.

في هذه الجلسة وحدها، **كل دفعة سلّمتها كشفت عطباً لم يظهر إلا عند
تشغيلها على نظامك**:

| الدفعة | العطب المكتشَف | لماذا لم أره قبلها |
|---|---|---|
| 1 | ترميز BOM في PowerShell | لا بيئة ويندوز عندي |
| 2b | `SET` بمعاملات مربوطة | لا قاعدة PostgreSQL عندي |
| 3 | `reranker` يبتلع 96% من الدرجة | لم يظهر إلا بعد قياس فعلي |
| 4 | الكيانات المزعجة كلها هوامش | لم يظهر إلا بعد فحص أمثلتها |

لو كدّست عليك 40 محركاً دفعة واحدة، فالنتيجة ليست مشروعاً مكتملاً،
بل كتلة كود تبدو صحيحة وتنهار عند أول تشغيل، ولا تعرف أي جزء منها
سبب الانهيار. **وهذا بالضبط ما تقول إنك لا تريده: "ما أمبي أشياء
تنعطب علي بعدين".**

الطريق إلى «أعلى المعايير» هو الدفعات المقيسة، لا الدفعة الواحدة
الكبيرة. وقد أثبتت الجلسة هذا عملياً.

---

## ثانياً: أين وصلنا فعلاً

| الطبقة | الحالة |
|---|---|
| الأسرار ونظافة المستودع | ✅ مكتمل |
| المطبّع العربي + خريطة الإزاحة | ✅ مكتمل، 32 اختبار |
| تصحيح OCR | ✅ يعمل — 65,655 تمديد، 3,285 كلمة مُلحمة |
| فهارس البحث | ✅ تُستعمل فعلاً لأول مرة |
| الترتيب RRF + شرح صادق | ✅ المجموع يطابق الدرجة بالضبط |
| العقود (21 عقداً) | ✅ مكتوبة ومُختبَرة |
| **محرك التخطيط** | ✅ **هذه الدفعة** — 30 اختبار |
| فلتر الكيانات | ⚠️ مكتوب، غير مربوط |
| Embeddings حقيقية | ❌ ما زالت hashing وهمية |
| Isnad / Narrator | ❌ لم يبدأ — التخطيط شرطه |
| Evidence / Verifier / FinalAnswer | ❌ العقود جاهزة، لا تنفيذ |
| Graph / Memory | ❌ لم يبدأ |
| Worker / Scheduler / Admin | ❌ لم يبدأ |

---

## ثالثاً: الترتيب المتبقي، بتقدير واقعي

### الدفعة 5 — المعجم والتقييم
أعد بناء المعجم من الصفر. الحالي مبني على نص قبل تصحيح OCR: تردد
الحرف "د" وحده 16,359 مقابل "محمد" الصحيحة 2,651. كل ما يعتمد عليه
(الاقتراحات، العبارات، الكيانات) يرث التلوث.
ثم وسّع المجموعة الذهبية إلى 30 سؤالاً محكَّماً منك.
**بدون هذا، كل ما بعده يُقاس بمسطرة معطوبة.**

### الدفعة 6 — Embeddings حقيقية
استبدال `EmbeddingBuilder` بنموذج متعدد اللغات، وقياسه على الذهبية.
وزن معيد الترتيب صفر الآن؛ يُرفع فقط بعد إثبات التحسّن رقمياً.

### الدفعة 7 — Isnad + Narrator
تعتمد كلياً على محرك التخطيط: استخراج السند من العناصر المصنَّفة
`sanad`، وتفكيكه إلى حلقات، وربط الرواة.
**تحتاج منك بيانات موسومة**: قائمة رواة معتمدة، وعيّنة أسانيد
مفكَّكة يدوياً للتحقق. لا يمكن بناء هذا من الكود وحده.

### الدفعة 8 — Evidence + Verifier + FinalAnswer
العقود جاهزة (`EvidenceBundle`, `VerificationResult`, `FinalAnswer`
الذي يرفض إجابة بلا مصدر). المطلوب تنفيذ خط الأنابيب.
هذه هي النقلة من «محرك بحث» إلى «منصة توثيق».

### الدفعة 9 — Graph + Memory
شبكة الرواة والأسانيد، ومقارنة النسخ.

### الدفعة 10 — التشغيل المؤسسي
Worker, Scheduler, Admin, Event bus, Audit log, RBAC, Monitoring.

---

## رابعاً: ما أحتاجه منك لأكمل

بعض هذا لا أستطيع إنتاجه مهما طال الوقت، لأنه يحتاج حكمك أنت:

1. **مراجعة عيّنة تصنيف التخطيط** — شغّل `-Review 30` واحكم عليها.
2. **20–30 سؤالاً ذهبياً** مع النتائج الصحيحة التي تحددها بنفسك.
3. **قائمة رواة معتمدة** لبناء Narrator.
4. **مراجعة قوائم الكلمات** في `entity_filter` و`ocr_corrector` —
   وضعت بذرة معقولة، لكنك المتخصص.

أرسل واحدة منها وأبني عليها. وكل دفعة أسلّمها بعدها ستكون مقيسة
قبل/بعد، لا موعودة.

---

## خامساً: ملاحظة أخيرة عن «أعلى المعايير»

أعلى المعايير في نظام كهذا ليس أكثر الشيفرة، بل:

- **لا ادعاء بلا دليل** — `FinalAnswer` يرفض إجابة بلا مصدر
- **لا تعديل صامت** — كل جزء من الدرجة مسجَّل في `score_explain`
- **لا حذف** — `text_raw` لا يُمس، والمرفوض يُحفظ مع سببه
- **لا قبول بلا قياس** — كل دفعة تُقارن بالأساس

هذه الأربعة مطبَّقة الآن فعلاً. وهي أثمن من عشرة محركات نصف مكتملة.
