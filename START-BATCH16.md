# الدفعة السادسة عشرة — محركات المواصفة الرئيسية

```powershell
cd E:\IslamicAI_v3_full
& .\.venv\Scripts\Activate.ps1
.\tools\G0-batch16.ps1 -DryRun
.\tools\G0-batch16.ps1
```

قرأتُ `IslamicAI_Final_Master_Spec.docx` كاملة. هذه الدفعة تبني
الأقسام **6.2 و 8 و 9** منها.

---

## ١) Evidence → Verifier → FinalAnswer

**هذا هو التحوّل الذي تطلبه**: من محرك بحث إلى منصة توثيق.

النتيجة لم تعد قائمة روابط، بل **حزمة أدلة** كل قطعة فيها موصولة
بموضعها الدقيق. والمحقق **يرفض الإجابة** حين لا تكفي.

مقيس على محاكاة مخرجاتك:

```
دليلان مستقلان صالحان
  [✔] وضوح النية       0.81
  [✔] عدد الأدلة       1.00   دليلان صالحان
  [✔] استقلال المواضع  1.00   موضعان مستقلان
  [✔] جودة النص        0.81
  [✔] الاتساق          1.00
  الحكم: answerable   ثقة 0.92
  « الماء يطهر ولا يطهر » — وسائل الشيعة ج1 ص134
```

```
دليل واحد رديء، نية غامضة
  الحكم: insufficient — لم يُجَب
  السبب: السؤال غير محدد النية؛ يلزم دليلان صالحان؛
         الأدلة من موضع واحد؛ جودة المسح منخفضة
```

**والتعارض لا يُرجَّح آلياً** (القسم 2: «لا حكم تلقائياً») — يُعرض
للمحقق بحكم `needs_review`.

## ٢) Narrator Engine — المعوّق الأول

قاموس بذرة **30 راوياً** من كبار رواة الكتب الأربعة. الربط بالاسم
المعتمد أو الكنية أو النسبة أو التقريب.

| الاسم من مخرجاتك | الربط |
|---|---|
| `محمد بن يعقوب` | محمد بن يعقوب الكليني |
| `ابن ابي عمير` | محمد بن أبي عمير |
| `علي بن ابراهيم` | علي بن إبراهيم القمي |
| `فلان بن فلان` | **unresolved** — لا يُخترع له معرّف |

**10 من 11 اسماً من مخرجاتك تُربط الآن.** والتوسيع **بيانات لا كود**:
`datasets/gazetteer/narrators.json`.

## ٣) Planner Engine — «الله» لم تعد تشغّل الدلالي

| الاستعلام | الخطة |
|---|---|
| `الله` (ثقة 0.20) | `fts` فقط **+ طلب توضيح** |
| `زرارة بن أعين` | `fts + fuzzy + gazetteer` — الأعلام لا تُطابَق دلالياً |
| `ما معنى البر` | `semantic + fts` — المفهوم لا يُطابَق حرفياً |
| `] 29214 [` | `fts` — الرقم يُطابَق أو لا |

الستون نتيجة الدلالية التي كانت تُقصى كلها لم تعد تُحسب أصلاً.

## ٤) Memory Engine — ذاكرة لا كاش

يخزّن **المؤكَّد وحده** (`answerable` بثقة ≥ 0.7)، **ويُبطل تلقائياً**
عند تغيّر أي إصدار في المسار — نتيجة بُنيت بمصحّح 1.2.0 لم تعد صالحة
بعد 1.3.0. وأي تصحيح بشري يمسح الذاكرة (القسم 9: Event-Driven).

## ٥) الحوكمة — القسم 9 كاملاً

- **RBAC**: أربعة أدوار (`guest` / `researcher` / `verifier` / `admin`)
  والرفض **معلَّل**: «الدور 'guest' لا يملك صلاحية 'reindex'»
- **Audit Log**: إلحاق فقط بصيغة JSONL، كل قرار بفاعله وسببه ووقته
- **Circuit Breakers**: ميزانية زمن ونتائج وتوسيع — «تجاوز عدد
  النتائج: 120» بدل استنزاف صامت

## ٦) إصلاحان من مخرجاتك

| العطب | الإصلاح |
|---|---|
| `، عن ابن ابي عمير` ككيان | تنظيف الحدود قبل الترشيح |
| `احمد بن محمد` مرتين (160 + 88) | توحيد → **248** مع حفظ الصيغتين |

---

**42 اختباراً جديداً ✅**

---

## السكربت يشغّل الخط كاملاً على قاعدتك

الخطوة 3 تأخذ استعلامين حقيقيين وتمرّرهما:

```
بحث → خطة → حزمة أدلة → تحقق → إجابة أو امتناع معلَّل
```

**أرسل لي مخرجاتها** — هي أول قياس حقيقي لخط التوثيق.

---

## ما بقي، بصراحة

| المحرك | الحالة |
|---|---|
| Graph Engine | يحتاج الرواة مربوطين أولاً — القاموس بذرة الآن |
| Ontology | يحتاج شجرة مفاهيم منك |
| Cross-Encoder | أيام عمل، لكن **بلا قياس لن نعرف إن حسّن** |
| Morphology | CAMeL Tools — الجذع الخفيف بديل مؤقت |
| Worker/Scheduler | بنية تشغيل مستقلة |

**والمحركات المبنية هنا تعمل مستقلةً ولم تُربط بالـ API بعد** — عمداً:
لتراها وتحكم عليها قبل أن تغيّر سلوك البحث. الربط الدفعة القادمة.
